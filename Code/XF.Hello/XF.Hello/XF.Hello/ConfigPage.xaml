<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XF.Hello.ConfigPage"
             Title="Configuração">
    <ContentPage.Content>
        <TableView>
            <TableView.Root>
                <TableSection Title="Privacidade">
                    <SwitchCell Text="Rastrear localização" On="{Binding Rastrear}" />
                    <SwitchCell Text="Permitir cookies" On="{Binding Permitir}" />
                </TableSection>
                <TableSection Title="Desempenho">
                    <SwitchCell Text="Disponibilizar no mural" On="{Binding Disponibilizar}" />
                    <SwitchCell Text="Enviar por e-mail" On="{Binding EnviarEmail}"/>
                    <SwitchCell Text="Receber sms" On="{Binding ReceberSMS}"/>
                </TableSection>
            </TableView.Root>
        </TableView>
    </ContentPage.Content>
</ContentPage>