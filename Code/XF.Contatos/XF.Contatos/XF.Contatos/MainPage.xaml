<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XF.Contatos"
             x:Class="XF.Contatos.MainPage"
             Title="Lista de contatos">

    <ContentPage.Content>
        <StackLayout Padding="5">
            <StackLayout Padding="5" Spacing="2">
                <Label Text="Lista de contatos"  />
            </StackLayout>
            <ListView ItemsSource="{Binding Contatos}" ItemTapped="lstContatos_ItemTapped" 
                      RowHeight="60"                      
                      IsPullToRefreshEnabled="True" Refreshing="lstContatos_Refreshing">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Nome}"  Margin="0,2,0,4" />
                                <Label Text="{Binding Telefone}"  />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>       

</ContentPage>
